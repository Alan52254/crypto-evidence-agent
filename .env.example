# 複製成 .env 並填入自己的值。.env 不會進版控。
#
# 三種模式，擇一即可 —— 都不設也能跑（推理層退回腳本、打分退回詞典）。

# ── 模式 A：地端模型（推薦，零成本、無額度限制）─────────────────
# 只要設 MODEL 就會啟用；BASE_URL 預設指向 Ollama。
#
#   Ollama            http://localhost:11434/v1
#   LM Studio         http://localhost:1234/v1
#   llama.cpp server  http://localhost:8080/v1
#
# 模型需支援 tool calling（function calling），否則蒐集迴圈無法動態決策。
# 已知可用：qwen3、llama3.1、mistral-nemo、firefunction-v2 等。
HOYABIT_LOCAL_MODEL=qwen3:8b
# HOYABIT_LOCAL_BASE_URL=http://localhost:11434/v1
# 勞務層（打分）可另外指定較小的模型；不設則與推理層共用。
# HOYABIT_LOCAL_LABOUR_MODEL=qwen3:1.7b
# 多數地端執行環境不驗證金鑰，留空即可。
# HOYABIT_LOCAL_API_KEY=

# ── 模式 B：雲端 Gemini ─────────────────────────────────────────
# 免費層申請：https://aistudio.google.com/apikey
# 注意：Pro 系列自 2026-04-01 起已移出免費層，只剩 Flash / Flash-Lite。
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3-flash
# GEMINI_LABOUR_MODEL=gemini-2.5-flash-lite

# ── 資料源 ──────────────────────────────────────────────────────
# Binance 公開端點與新聞 RSS 都不需要金鑰，因此這裡沒有必填項。
