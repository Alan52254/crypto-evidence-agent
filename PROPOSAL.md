# HoyaBit 黑客松提案
## 可溯源的加密貨幣分析 Agent — 基於 ReAct 架構的證據推理系統

**日期**：2026-07-23 ｜ **期程**：4 週 ｜ **團隊**：2-3 人

---

## 一、一句話定位

**市面上的 AI 分析工具給你一段沒有來源的文字；我們給你一份每句話都能追回原始出處的分析，
以及它是怎麼想出來的完整過程。**

這不是「再做一個投顧 ChatBot」。差異在三個地方：

1. **每一句判斷強制掛載證據識別碼**，追不回來源的句子在組裝階段就被系統丟棄，進不了報告。
2. **推論軌跡是交付物**，不是除錯副產品 —— 評審能看到模型在哪一步發現證據不足、
   決定回頭補抓、以及那個決定如何改變了最終結論。
3. **信心度衡量的是證據之間的分歧程度**，不是模型自己說它有幾成把握。
   當技術面看空、社群看多時，系統會明說「這裡有分歧」，而不是硬給一個漂亮結論。

---

## 二、如何回答命題的三個範例

命題給的三個範例看似是三種功能，實際上是**一條 pipeline 加一層**：

| 範例 | 我們的答案 | 成本 |
|---|---|---|
| **一、分析某幣種的表現** | 核心單幣分析 pipeline | 主體工作 |
| **二、多空的結論從哪裡來** | **零額外成本** —— 輸出契約已強制逐句掛證據，這是 pipeline 的副產品而非新功能 | 0 |
| **三、哪個幣種比較值得關注** | 同一條 pipeline fan-out 五次 + 排序層。**4 週內不排進計畫，但架構已預留** | 後續 1-2 天 |

第三項的設計保障寫在規格裡：單幣分析必須是**純函數、輸出結構化結論物件**，
因此加入比較層是外掛而非重構。這是刻意的取捨 —— 我們選擇把 4 週押在
「一條路走到深」而非「三條路都走一半」。

---

## 三、技術架構

### 3.1 整體形狀：三個接縫

整套系統刻意只有三個接縫（可替換點），這是為了讓 2-3 人在 4 週內能並行推進而不互相踩線：

```
                    ┌─────────────────────────────────────┐
   分析請求 ──────►  │        分析回合 (接縫 2)             │ ──► 分析報告
                    │  幣種閘門 → 規劃 → 蒐集迴圈          │ ──► 推論軌跡
                    │  → 缺口檢查 → 組裝 → 引用檢核        │
                    └───────┬─────────────────┬───────────┘
                            │                 │
                   ┌────────▼───────┐  ┌──────▼─────────┐
                   │ 證據源 (接縫 1) │  │ 模型供應者(接縫3)│
                   │  MCP 工具       │  │ 推理層 / 勞務層 │
                   └────────────────┘  └────────────────┘
```

**關鍵效果**：所有測試都打接縫 2，在接縫 1 塞假證據源、接縫 3 塞腳本假模型
→ **整條 pipeline 不碰網路、不燒 token 就能完整測試**。CI 跑得動，開發不必等 API。

### 3.2 雙軌工具：切分判準只有一條

工具分成 **MCP 工具**與 **Function 工具**，判準唯一 —— **有沒有跨行程 I/O**：

| | MCP 工具 | Function 工具 |
|---|---|---|
| **判準** | 有外部 I/O | 純計算、無 I/O |
| **內容** | 各證據源存取、向量檢索 | 技術指標計算、情緒彙總、證據去重、信心度計算、引用檢核 |
| **測試** | 以假適配器替換 | **無需任何 mock** |

**為什麼是這條線**：因為它同時是三件事 ——
**測試邊界**（Function 側全部可無 mock 測試）、
**故障邊界**（外部源掛掉只影響 MCP 側，推理不崩）、
**部署邊界**（MCP server 可獨立掛載到 Kiro / Claude Desktop，現場即席驗證資料層是真的）。

若改用「內部知識 vs 外部資訊」切，最重 I/O 的市場 API 會落進 Function 側，三者同時瓦解。

### 3.3 推論引擎：固定骨架 + 迴圈內動態

這是本提案最需要說清楚的設計取捨。

**純固定管線**（「先做 A，再做 B，最後做 C」）跑得完，但那不是推理 ——
評審會直接問「這跟寫死的流程差在哪」。
**純動態 ReAct** 是真推理，但有真實風險在評審面前繞迴圈或超時。

我們的答案是分層：

- **外層是固定的狀態圖**（規劃 → 蒐集 → 缺口檢查 → 組裝），由 LangGraph 管理狀態與 checkpoint。
  **保證一定收斂。**
- **蒐集迴圈內部是真正的 ReAct**：呼叫哪些工具、要不要再挖一輪，
  全由模型依據**當前的證據缺口**決定。

「證據缺口」是這個設計的樞紐 —— 它把「模型該做什麼」從一段模糊的 prompt
變成一個**可計算、可顯示、可斷言**的狀態。評審在軌跡上看到的不是
「模型說它要去抓資料」，而是「情緒面證據為 0 → 模型選擇補抓 → 缺口歸零」。

### 3.4 時間預算：15 分鐘是上限，不是目標

預算由分析回合持有，每個軌跡節點扣減。**預算耗盡時走「以現有證據組裝」路徑，
永不回傳逾時錯誤** —— 一份低信心度的報告優於沒有報告。

---

## 四、資料層

### 4.1 混合式：背景 ingestion + 即時補齊

背景服務持續蒐集五個受涵蓋幣種的新聞與市場數據進向量庫；
分析時檢索歷史脈絡，再以即時 API 補當下數值。
只有五個幣種，範圍窄、成本可控。

⚠️ **這意味著 ingestion 必須在 Demo 前一到兩週就上線運行**，否則向量庫是空的。
這是排程上的硬需求，已列入時程。

### 4.2 資料源與優先序

| 來源 | 額度 | 金鑰 | 角色 |
|---|---|---|---|
| **Binance 公開 REST** | 以 IP 計 weight | **不需要** | **輪詢主力**：K 線、盤口 → 技術面、籌碼面 |
| **新聞 RSS**（Cointelegraph、CoinDesk） | 無限制 | **不需要** | **性價比最高**：天然帶時間戳與原文連結，利於溯源 → 基本面、情緒面 |
| **CoinGecko Demo** | 100/min、**10,000/月** | 免費申請 | **僅補充欄位，不可作輪詢主力**（月額度除以 30 天僅 333 次/日） |
| **CoinGlass** | 免費層 | 待確認 | 籌碼面：交易所淨流入流出、資金費率、未平倉合約 |
| **本所聚合指標** | — | 內部 | **4 週內只做介面與假資料**，避免時程被內部開通流程綁架 |
| Reddit | 100 QPM (OAuth) | ⚠️ **人工核准 2-4 週** | **不列為關鍵路徑**（見風險章節）|

**合乎命題約束**：所有來源回傳的都是**原始數值序列或原文**，
不含任何現成結論或評分。所有結論由我們自己的 Function 工具計算，或由推理層從證據推出。
驗收時會逐一資料源複查此約束。

### 4.3 儲存：PostgreSQL + pgvector（而非純向量庫）

證據溯源本質上是一連串 join：**判斷 → 證據識別碼 → 來源片段 → 出處與時間**。
這是 SQL 擅長、純向量庫不擅長的。用 Postgres 一套解決向量與關聯式，
省掉「兩個儲存體資料不一致」這個 4 週內最不划算的問題。
資料量（五幣種、四週）遠低於 pgvector 的實用上限。

---

## 五、模型策略與評估基準

### 5.1 分層，而非二選一

ReAct 迴圈裡有兩種完全不同的工作：

- **推理層** —— 決定下一步、整合矛盾證據、撰寫報告。能力天花板直接決定成敗。
- **勞務層** —— 單則貼文情緒打分、去重、摘要。呼叫次數是推理層的數十倍，但每次都簡單。

混用同一顆大模型會吃光 15 分鐘的壁鐘預算並撞速率限制。

| 層 | 選型 | 價格（每百萬 tokens 輸入/輸出）|
|---|---|---|
| 推理層 | Gemini 3.1 Pro 或 Claude Sonnet 5 | $2/$12 ｜ $3/$15 |
| 勞務層 | Gemini 2.5 Flash-Lite | $0.10/$0.40 |

兩層都藏在同一個模型供應者介面後，可獨立替換。

> **註**：Google AI Studio 免費層自 2026-04-01 起已移除 Pro 系列，
> 推理層必須編列付費預算。但依命題「token 消耗不是關注重點」，
> 五幣種單次分析的實際花費為個位數美元等級，非決策因素。

### 5.2 雲端 vs 地端微調：評估基準

我們提出四項**可量化的門檻**作為評估基準，並將實際跑出成績單（見 ticket 11）：

| 基準 | 門檻 | 為何選它 |
|---|---|---|
| **多輪 tool calling 成功率** | ≥95%（10 步內不迷失、不重複呼叫）| ReAct 迴圈成敗直接由此決定 |
| **單次分析壁鐘時間** | ≤15 分鐘 | 命題硬性規定 |
| **引用忠實度** | 抽樣判斷，掛載的證據確實支撐該句 ≥90% | 輸出契約的核心，也是評審看的東西 |
| **單次分析成本** | ≤US$1 | 非決策因素，但要有數字證明可規模化 |

**結論：推理層在 4 週內不做微調。**

理由不是「微調沒用」，而是**微調無法改善最關鍵的那一項**（多輪 tool calling 成功率）。
另外需要澄清一個常見誤解：「地端微調在格式遵循上優於雲端」在 2026 年已不成立 ——
現役旗艦模型都支援 structured output / JSON schema 強制約束。
微調現在還站得住的理由只剩三個：**資料不能出境、極高頻的單位成本、極低延遲**。

勞務層則相反 —— 高頻、簡單、格式固定，正是微調或自架 Gemma（開放權重，$0/token）
真正划算的地方。這是後續優化的方向。

### 5.3 關於 AWS Kiro 的定位澄清

**Kiro 是 spec-driven 的 agentic IDE（開發工具），不是可呼叫的 LLM，沒有 inference API。**
它不能、也不需要擔任系統的推理主腦。

競賽提供的 2000 credits 約當 US$80 的**開發**額度（超額 $0.04/credit）。
正確的加分方式是**在開發過程中真的使用 Kiro 的 spec / hooks / agents，並在提案中展示這些產出物**
—— 而不是宣稱系統主腦是它。我們的 MCP server 設計（ticket 10）也讓資料層可直接掛載進 Kiro，
現場即席示範。

---

## 六、可信度設計（三道機制）

### 6.1 幣種閘門：白名單，不是黑名單

公司要求不碰迷因幣與水幣。**但「水幣」寫不成程式判準** ——
黑名單永遠追不完，明天出的新幣就漏了。

我們的做法是把它反過來定義：系統**不判斷任何資產是不是水幣**，
只判斷它**在不在五個受涵蓋幣種的集合內**。這是一行集合比對，零漏網，
且在推理開始**之前**執行 —— 被拒絕的請求不消耗任何預算。

這也不靠 prompt 拜託模型守規矩，是寫死的程式閘門。

### 6.2 引用檢核：無證據的句子進不了報告

組裝階段逐句檢查是否掛載有效證據識別碼。未通過者**直接丟棄，不進報告，
但進推論軌跡** —— 讓評審親眼看到檢核確實在運作，而不只是聽我們宣稱。

> 我們刻意不宣稱「零幻覺」。強制引用大幅降低無據生成，
> 但誠實的說法是：**沒有證據的句子出不了系統**。

### 6.3 信心度：衡量分歧，而非模型的自我感覺

「信心度」有三種可能定義，我們明確選了第三種（並寫成 ADR）：

| 定義 | 為何不選 |
|---|---|
| 模型自陳的把握 | 不可驗證，且讓信心度變成又一個會幻覺的欄位 —— 與整份輸出契約矛盾 |
| 歷史回測準確率 | 誠實，但 4 週內做不到，且加密貨幣方向性回測極易過擬合 |
| **✅ 證據面之間的一致程度** | **唯一可從證據直接計算、可被稽核、且本身也可溯源的定義** |

四個證據面（技術面／籌碼面／基本面／情緒面）各自產出方向傾向，
信心度衡量它們彼此同意的程度。四面一致 → 高；技術面看空但社群強烈看多 → 低。

**「證據不足」是與「低信心度」不同的第三態。** 只蒐到一個面時，
信心度是「無法計算」而非「高」—— 否則會出現「只查到一則新聞 → 四面一致 → 高信心」的荒謬結果。

**業務接合點**：多空分歧、信心度低，正好是「用定期定額分散進場時點」
在邏輯上**真正成立**的時候。報告可以在此自然帶到 HoyaBit 最低 70 元的定期定額 ——
這不是硬推銷，是分析結論本身指向的行動。

---

## 七、推論軌跡 —— 差異化核心

命題原文：「要能看到不同 Agent 架構下，**他的點對點之間為什麼結果這樣出來**」、
「不是只是問一個投顧」。

我們把這句話當成**主要交付物**來設計，而非事後補的 log。

**為什麼自建而非用現成平台**：LangSmith / Langfuse 幾乎零工就有完整 trace 樹，
但它們呈現的是**工程師視角的 span 與延遲**，不是「證據如何支撐結論」。
評審從一棵 span 樹看不出推理質地。我們要的畫面是**證據與判斷之間的連線**，
這需要自己定義 schema。

軌跡前端呈現：

- 時間軸與樹狀兩種視圖，每個節點顯示：**動作理由、輸入、產出證據、缺口變化**
- **證據 → 判斷的連線可視化** —— 「點對點之間為什麼」的具體兌現
- 點擊任一證據展開原始片段、出處 URL、擷取時間
- **顯示被引用檢核丟棄的判斷及原因**
- 15 分鐘預算的消耗分布

---

## 八、技術棧（版本已鎖定）

```toml
requires-python  = ">=3.11,<3.13"

langgraph        = "==1.2.9"      # ReAct 狀態機、durable execution、checkpoint
langchain        = "==1.3.14"     # 僅用其 model / tool 抽象
llama-index-core = "==0.14.23"    # RAG ingestion 與檢索
mcp              = "==1.28.1"     # ⚠️ 不得升 2.x，見下
```

儲存：**PostgreSQL 16+ 搭配 pgvector**

> **LangGraph 與 LangChain 不是二選一。** LangGraph 是低階的有狀態編排框架，
> 負責 ReAct 狀態機與 checkpoint；LangChain 提供上層模型與工具抽象。兩者相輔。

> ⚠️ **MCP 版本風險**：MCP 規格 2026-07-28 版是 launch 以來最大改版
> （stateless core、Extensions framework、Tasks、MCP Apps、authorization hardening），
> Python SDK v2 是為它重寫的，目前僅 alpha/beta。
> 4 週專案必須 pin `mcp==1.28.1` 並在 `pyproject.toml` 寫死上界。

---

## 九、時程與工作切分（11 張票）

工作切成 11 張**曳光彈式垂直切片** —— 每張票都貫穿所有層、可獨立展示，
而非「這週做資料層、下週做 Agent 層」。

```
週次   票                                          可並行
─────────────────────────────────────────────────────────
W1    01 端到端骨架（假證據源 + 假模型）           ← 全隊先合力打通
      ├─ 02 持久化 (Postgres + pgvector)
W1-2  ├─ 03 Binance 證據源（技術面/籌碼面）        ← 三人並行
      ├─ 04 新聞 RSS 證據源（基本面/情緒面）
      └─ 05 模型供應者（推理層 + 勞務層）
W2-3  06 蒐集迴圈動態化 + 15 分鐘預算   ← 需 01,03,04,05
      08 背景 ingestion + 向量檢索      ← 需 02,03,04 ⚠️ 須提前上線
      09 推論軌跡視覺化前端             ← 需 02，可與 06 並行
W3    07 報告組裝 / 引用檢核 / 信心度    ← 需 06
      10 MCP server 封裝                ← 需 03,04
W4    11 評估基準與成績單               ← 需 07
      緩衝 + Demo 排練
```

**第一張票（端到端骨架）是全案的樞紐** —— 它一次確立三個接縫、測試慣例、
報告與軌跡的資料形狀。它完成之後，後續每張票都只是「把某個假的部分換成真的」，
三個人可以並行而不衝突。

---

## 十、風險與對策

| 風險 | 嚴重度 | 對策 |
|---|---|---|
| **Reddit OAuth 人工核准需 2-4 週**，等於整個專案期 | 高 | **今天立即送申請**；情緒層主力改為新聞 RSS 全文 + WebSearch；Reddit 放在證據源介面後當可選源，核准就插上，沒核准不影響 Demo。（另註：交易所屬商業用途，嚴格說不適用免費非商業層）|
| **MCP 規格 2026-07-28 破壞性改版** | 中 | `pyproject.toml` 寫死上界，4 週內不升 2.x |
| 背景 ingestion 未提前上線 → 向量庫空、RAG 無物可撈 | 中 | ticket 08 排在 W2，Demo 前至少運行一到兩週 |
| ReAct 迴圈在評審面前繞不出來或超時 | 中 | 固定骨架保證收斂 + 預算耗盡走「以現有證據組裝」，**永不逾時錯誤** |
| 單一資料源失效毀掉整場 Demo | 中 | 證據源失效以**空集合**表達而非例外；分析回合繼續，缺口反映於信心度 |
| CoinGecko 月額度 10k 被輪詢燒光 | 低 | 明定 Binance 為輪詢主力，CoinGecko 僅補充欄位 |
| CoinGlass 免費層實際額度未知 | 低 | W1 查證；查不到則籌碼面改以 Binance 盤口數據為主 |

---

## 十一、明確排除的範圍（以及為什麼）

這些是**刻意的工程取捨，不是遺漏**：

| 排除項 | 理由 |
|---|---|
| **任何交易執行能力** | 不把會產生幻覺的 LLM 接上真實資金操作。系統只輸出分析，不動用任何資金 |
| **金寶桌寵前端 / ChatBot 路由** | 4 週內與核心推理能力爭搶資源，且不是命題的評分點 |
| **Kubernetes + Prometheus + Grafana** | Grafana 秀的是 span 延遲，不是「證據如何支撐結論」，與評分點錯位。可觀測性全押自建推論軌跡 |
| **跨幣種比較層** | 架構已預留（單幣分析是純函數、輸出結構化物件），後續 1-2 天可加 |
| **本所聚合指標真實串接** | 授權已取得，但只做介面與假資料，避免時程被內部開通流程綁架 |
| **地端模型微調** | 微調無法改善最關鍵的多輪 tool calling 成功率（見 5.2）|

---

## 十二、與 HoyaBit 業務的接合

本系統雖以競賽為第一優先，但有三處按生產規格設計，使其可長成產品：

1. **資料層** —— 證據源介面統一，新增來源只需實作一個介面。
   本所聚合指標（分幣種成交量、買賣盤比、定期定額淨流入、新增持倉帳戶數）的接口已預留，
   打開即可讓報告寫出**全場只有 HoyaBit 寫得出來的句子** ——
   「本所定期定額買盤在 BTC 回檔時淨流入增加」。
2. **輸出契約** —— 產品化時只需在**最外層**換措辭並加免責聲明，內核完全不動。
   報告本身已刻意避開「建議」「訊號」等帶投資顧問意涵的詞，只說「方向」。
3. **信心度 → 定期定額** —— 低信心度（多空分歧）在邏輯上正是分批進場成立的時候，
   報告可自然引導至 HoyaBit 最低 70 元的定期定額，而非硬性推銷。

---

## 附錄：完整文件索引

| 文件 | 內容 |
|---|---|
| [`docs/decisions/0001-grill-decisions.md`](docs/decisions/0001-grill-decisions.md) | 十輪決策紀錄與理由 |
| [`docs/research/0001-tech-stack-facts.md`](docs/research/0001-tech-stack-facts.md) | 版本號、資料源額度、模型定價的一手來源查證 |
| [`CONTEXT.md`](CONTEXT.md) | 領域術語表（證據、證據面、判斷、方向、信心度、推論軌跡…）|
| [`docs/adr/0002-confidence-is-cross-facet-agreement.md`](docs/adr/0002-confidence-is-cross-facet-agreement.md) | 信心度定義的決策與被否決的替代方案 |
| [`docs/spec/0001-analysis-agent.md`](docs/spec/0001-analysis-agent.md) | 完整規格：26 條 user story、實作決策、測試決策 |
| [`docs/design/0001-module-shapes.md`](docs/design/0001-module-shapes.md) | 模組形狀、三個接縫的完整介面、深度分析 |
| [`.scratch/analysis-agent/issues/`](.scratch/analysis-agent/issues/) | 11 張票，含 blocking edges 與驗收條件 |
